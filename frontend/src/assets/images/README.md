# Image assets

Place image files imported by React components in this directory. Vite will
fingerprint and bundle them for production.

Use `frontend/public/` only for files that must retain a fixed public URL.
